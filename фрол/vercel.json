import { defineConfig } from 'vite'

export default defineConfig({
  root: 'фрол/templates',       // корневая папка — где твой main.html
  build: {
    rollupOptions: {
      input: 'фрол/templates/main.html',  // входной HTML-файл
    }
  }
})
